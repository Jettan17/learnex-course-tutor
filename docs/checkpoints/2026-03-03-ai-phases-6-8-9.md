# Checkpoint: ai-phases-6-8-9

Created: 2026-03-03 (end of session)
Git SHA: c73f750
Branch: main

## Summary

Completed AI integration phases 6, 8, and 9. Also fixed the topic detail page design system to match the homepage glassmorphism style.

## Files Changed (since last checkpoint: 761ad45)

### New Files
- `app/src/lib/ai.ts` - Gemini 2.0 Flash model setup (chatModel + hintModel)
- `app/src/types/ai.ts` - ChatMessage and HintResponse types
- `.env.local.example` - GEMINI_API_KEY setup docs with Claude upgrade path
- `app/src/app/api/topic-chat/route.ts` - Streaming POST, injects topic context as system prompt
- `app/src/app/api/hint/route.ts` - Non-streaming POST, 2-level Socratic hints
- `app/src/components/TopicChat.tsx` - Embedded chat panel, suggested questions, code rendering
- `app/src/components/questions/QuestionHint.tsx` - 2-level progressive hint UI

### Modified Files
- `app/package.json` - Added `ai`, `@ai-sdk/google`, `@ai-sdk/react`
- `app/src/app/courses/[courseId]/[topicId]/page.tsx` - Full design system overhaul + TopicChat integration
- `app/src/app/courses/[courseId]/[topicId]/quiz/page.tsx` - QuestionHint integration, FSRS hint penalty

## Completed Phases

| Phase | Description | Status |
|-------|-------------|--------|
| 6 | AI Infrastructure (Vercel AI SDK + Gemini) | ✅ Done |
| 8 | Topic Q&A Chat Widget | ✅ Done |
| 9 | Quiz Hint System | ✅ Done |
| 10 | UI color fix (courseId page) | ✅ Done (prev session) |
| Topic page UI | topicId page design fix | ✅ Done (this session) |

## Remaining User Action

- Add `GEMINI_API_KEY` to Vercel environment variables
  - Get free key at: https://aistudio.google.com
  - Vercel → Project → Settings → Environment Variables → Add `GEMINI_API_KEY`
  - Redeploy for changes to take effect

## Notes

- `useChat` in AI SDK v6 is from `@ai-sdk/react` (not `ai/react` as in v5)
- Topic chat is embedded inline (not floating) — cleaner UX than fixed bottom-right
- Hint system uses FSRS rating 2 (Hard) instead of 3 (Good) when correct with hint
